<%= form_with(model: phone, local: true) do |form| %>
  <% if phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number, id: :phone_phone_number %>
  </div>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.collection_select(:account_id, Account.all, :id, :subscriber_name) %>
  </div>

  <div class="actions">
    <%= form.submit 'Create Phone', :class => 'btn btn-primary' %>
  </div>
<% end %>
